package main

import "net/http"

func add(w http.ResponseWriter, r *http.Request) {
	// allow a channel to be inserted when the correct preimage is delivered
}

func get(w http.ResponseWriter, r *http.Request) {
	// since := r.URL.Query().Get("since")
}
